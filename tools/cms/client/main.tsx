import React, { useEffect, useState } from 'react';
import { createRoot } from 'react-dom/client';
import { ArticleEditor } from '../editor/ArticleEditor';
import type { Post, PostInput } from '../lib/types';
import './style.css';

function Icon({name,size=18}:{name:string;size?:number}) { const paths:Record<string,React.ReactNode>={
  posts:<><rect x="4" y="3" width="16" height="18" rx="3"/><path d="M8 8h8M8 12h8M8 16h5"/></>,search:<><circle cx="10.5" cy="10.5" r="6.5"/><path d="m16 16 4 4"/></>,plus:<path d="M12 5v14M5 12h14"/>,arrow:<path d="m8 5 7 7-7 7"/>,out:<><path d="M14 4h6v6M20 4 10 14"/><path d="M10 4H5a1 1 0 0 0-1 1v14a1 1 0 0 0 1 1h14a1 1 0 0 0 1-1v-5"/></>,folder:<path d="M3 7V5a1 1 0 0 1 1-1h5l2 3h9a1 1 0 0 1 1 1v11H3Z"/>,check:<path d="m5 12 4 4L19 6"/>,clock:<><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/></>,settings:<><circle cx="12" cy="12" r="8"/><circle cx="12" cy="12" r="3"/></>,back:<path d="m12 5-7 7 7 7M5 12h15"/>,trash:<><path d="M3 6h18M9 6V3h6v3M6 6l1 15h10l1-15M10 10v7M14 10v7"/></>};return <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.6" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">{paths[name]||paths.posts}</svg>}
async function api<T>(url:string,method='GET',data?:unknown):Promise<T>{const res=await fetch(url,{method,headers:{'Content-Type':'application/json','X-Karma-Request':'1'},body:data===undefined?undefined:JSON.stringify(data)});const result=await res.json();if(!res.ok)throw new Error(result.error||'Request failed');return result;}
const blank=():PostInput=>({id:'',title:'',description:'',pubDate:new Date().toISOString().slice(0,10),draft:true,tags:[],body:''});
const date=(value:string)=>new Date(value).toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric',timeZone:'UTC'});
function App(){
  const [posts,setPosts]=useState<Post[]>([]),[issues,setIssues]=useState<{id:string;message:string}[]>([]),[config,setConfig]=useState({contentDir:'src/content/blog',mediaDir:'public/media',siteUrl:'http://localhost:4321'});
  const [loading,setLoading]=useState(true),[error,setError]=useState(''),[notice,setNotice]=useState(''),[query,setQuery]=useState(''),[filter,setFilter]=useState('all'),[sort,setSort]=useState('updated'),[page,setPage]=useState('posts');
  const [post,setPost]=useState<PostInput|null>(null),[isNew,setIsNew]=useState(false),[baseline,setBaseline]=useState(''),[mode,setMode]=useState<'visual'|'source'>('visual'),[editorKey,setEditorKey]=useState(0),[busy,setBusy]=useState(false);
  const dirty=post!==null&&JSON.stringify(post)!==baseline;
  const refresh=async()=>{const result=await api<{posts:Post[];errors:typeof issues}>('/api/posts');setPosts(result.posts);setIssues(result.errors);};
  useEffect(()=>{Promise.all([refresh(),api<typeof config>('/api/config').then(setConfig)]).catch(e=>setError(e.message)).finally(()=>setLoading(false));},[]);
  useEffect(()=>{const leave=(e:BeforeUnloadEvent)=>{if(dirty){e.preventDefault();e.returnValue='';}};window.addEventListener('beforeunload',leave);return()=>window.removeEventListener('beforeunload',leave);},[dirty]);
  useEffect(()=>{if(!notice)return;const t=setTimeout(()=>setNotice(''),4000);return()=>clearTimeout(t);},[notice]);
  const canLeave=()=>!dirty||confirm('Discard your unsaved changes?');
  const navigate=(next:string)=>{if(!canLeave())return;setPost(null);setPage(next);setError('');};
  const open=async(id?:string)=>{if(!canLeave())return;setBusy(true);try{const next=id?await api<Post>(`/api/post?id=${encodeURIComponent(id)}`):blank();setPost(next);setBaseline(JSON.stringify(next));setIsNew(!id);setMode('source');setEditorKey(x=>x+1);setPage('posts');setError('');}catch(e){setError((e as Error).message);}finally{setBusy(false);}};
  const change=(patch:Partial<PostInput>)=>setPost(p=>p?{...p,...patch}:p);
  const save=async()=>{if(!post)return;setBusy(true);setError('');try{const next=await api<Post>(isNew?'/api/posts':'/api/post',isNew?'POST':'PUT',post);setPost(next);setBaseline(JSON.stringify(next));setIsNew(false);setNotice('Article saved to your repository');await refresh();}catch(e){setError((e as Error).message);}finally{setBusy(false);}};
  const remove=async()=>{if(!post||!confirm(`Permanently delete “${post.title}” from your repository?`))return;setBusy(true);try{await api('/api/post','DELETE',{id:post.id,revision:post.revision});setPost(null);await refresh();setNotice('Article deleted');}catch(e){setError((e as Error).message);}finally{setBusy(false);}};
  const published=posts.filter(p=>!p.draft).length;
  const shown=posts.filter(p=>(filter==='all'||(filter==='drafts'?p.draft:!p.draft))&&`${p.title} ${p.description} ${p.tags.join(' ')}`.toLowerCase().includes(query.toLowerCase())).sort((a,b)=>sort==='title'?a.title.localeCompare(b.title):sort==='date'?b.pubDate.localeCompare(a.pubDate):b.updatedAt.localeCompare(a.updatedAt));
  return <div className="shell"><aside className="sidebar"><a className="brand" href="#" onClick={e=>{e.preventDefault();navigate('posts');}}><span className="brandmark">✳</span><span>karma<span className="cms-label">CMS</span></span></a><div className="workspace"><div className="workspace-icon">K</div><div><strong>My workspace</strong><small>Astro website</small></div><span className="local-dot"/></div><p className="nav-label">WORKSPACE</p><button className={`nav-item ${page==='posts'?'selected':''}`} onClick={()=>navigate('posts')}><Icon name="posts"/> Articles <span className="nav-count">{posts.length}</span></button><button className={`nav-item ${page==='settings'?'selected':''}`} onClick={()=>navigate('settings')}><Icon name="settings"/> Settings</button><div className="sidebar-bottom"><div className="local-note"><span className="local-dot"/><strong>Local by design</strong><p>Your content stays in your repo.<br/>No cloud. No middleman.</p></div><a className="nav-item" href={config.siteUrl} target="_blank" rel="noreferrer"><Icon name="out"/> Open website <span>↗</span></a><div className="sidebar-footer"><span className="avatar">K</span><span>Karma CMS<small>Your writing space</small></span><span className="version">v1.0</span></div></div></aside>
  <div className="main"><header className="topbar"><div>Workspace <span>/</span> <strong>{page==='settings'?'Settings':'Articles'}</strong>{post&&<><span>/</span><strong>{isNew?'New article':'Edit article'}</strong></>}</div><span className="environment"><span className="local-dot"/> Local environment</span></header>
  {error&&<div className="alert" role="alert">{error}<button onClick={()=>setError('')} aria-label="Dismiss error">×</button></div>}{notice&&<div className="toast" role="status"><Icon name="check"/>{notice}</div>}
  {page==='settings'?<section className="content"><p className="eyebrow">YOUR WORKSPACE</p><h1>Simple by nature.</h1><p className="subtitle">A local writing tool. A repository you own.</p><div className="settings-card"><h2>Repository configuration</h2><dl><dt>Content directory</dt><dd><code>{config.contentDir}</code></dd><dt>Media directory</dt><dd><code>{config.mediaDir}</code></dd><dt>Website</dt><dd>{config.siteUrl}</dd><dt>Content format</dt><dd>Markdown + YAML frontmatter</dd><dt>CMS server</dt><dd>http://localhost:4000 · loopback only</dd></dl><p>Edit <code>cms.config.ts</code> and restart the CMS to change these settings.</p><p>The Astro collection schema lives in <code>src/content.config.ts</code>. The website does not depend on Karma CMS.</p></div></section>:post?<section className="content editor-page"><div className="page-heading"><div><button className="back" onClick={()=>navigate('posts')}><Icon name="back"/> All articles</button><h1>{isNew?'A new beginning.':'Make every word count.'}</h1></div><div className="heading-actions"><span className="save-state">{dirty?'Unsaved changes':'All changes saved'}</span><button className="primary" onClick={save} disabled={busy}><Icon name="check"/>{busy?'Saving…':'Save article'}</button></div></div><div className="editor-grid" inert={busy}><div className="writing-card"><input className="title-input" aria-label="Article title" placeholder="Give your story a title…" value={post.title} maxLength={250} onChange={e=>{const title=e.target.value;change({title,...(isNew&&(!post.id||post.id===post.title.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,''))?{id:title.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')}:{} )});}}/><textarea className="description-input" aria-label="Description" placeholder="A short description that invites people in…" value={post.description} onChange={e=>change({description:e.target.value})}/><div className="editor-mode"><div><button className={mode==='visual'?'active':''} onClick={()=>{if(mode==='visual')return;if(post.body&& !confirm('Visual editing supports common Markdown. Complex syntax may be normalized when edited. Continue?'))return;setMode('visual');setEditorKey(x=>x+1);}}>Visual editor</button><button className={mode==='source'?'active':''} onClick={()=>setMode('source')}>Markdown</button></div><span>{post.body.trim().split(/\s+/).filter(Boolean).length} words</span></div>{mode==='source'?<textarea className="source-editor" aria-label="Markdown body" placeholder="Your story starts here…" value={post.body} onChange={e=>change({body:e.target.value})}/>:<ArticleEditor key={editorKey} body={post.body} onChange={body=>change({body})}/>}<div className="editor-foot"><span className="local-dot"/> Saved as a plain Markdown file</div></div><aside className="properties"><h3>Article details</h3><label>Status<select value={post.draft?'draft':'published'} onChange={e=>change({draft:e.target.value==='draft'})}><option value="draft">Draft</option><option value="published">Published</option></select></label><p className="field-help">Published articles appear on the next Astro build.</p><label>File name<input value={post.id} disabled={!isNew} onChange={e=>change({id:e.target.value})} placeholder="my-first-story"/></label><p className="field-help">{config.contentDir}/{post.id||'your-story'}.md</p><label>Publication date<input type="date" value={post.pubDate} onChange={e=>change({pubDate:e.target.value})}/></label><label>Tags<input value={post.tags.join(',')} placeholder="Design, Notes" onChange={e=>change({tags:e.target.value?e.target.value.split(','):[]})}/></label><p className="field-help">Separate tags with commas.</p>{!isNew&&<><button className="secondary full" disabled={busy} onClick={()=>open(post.id)}><Icon name="clock"/> Reload from disk</button><button className="danger full" disabled={busy} onClick={remove}><Icon name="trash"/> Delete article</button></>}</aside></div></section>:<section className="content"><div className="page-heading"><div><div className="eyebrow">A LITTLE SPACE FOR BIG IDEAS</div><h1>Your words, at home.</h1><p className="subtitle">A place to write, refine, and share what matters.</p></div><button className="primary" onClick={()=>open()} disabled={busy}><Icon name="plus"/> New article</button></div><div className="stats"><div><span className="stat-icon"><Icon name="posts"/></span><div><span>Total articles</span><strong>{posts.length.toString().padStart(2,'0')}</strong></div><span className="stat-hint">Your growing collection</span></div><div><span className="stat-icon green"><Icon name="check"/></span><div><span>Published</span><strong>{published.toString().padStart(2,'0')}</strong></div><span className="stat-hint">Out in the world</span></div><div><span className="stat-icon amber"><Icon name="clock"/></span><div><span>Drafts</span><strong>{(posts.length-published).toString().padStart(2,'0')}</strong></div><span className="stat-hint">Ideas in the making</span></div></div><div className="library-heading"><h2>All articles <span>{posts.length}</span></h2><span><Icon name="folder" size={14}/>{config.contentDir}</span></div><div className="library"><div className="library-toolbar"><div className="tabs">{[['all','All articles'],['published','Published'],['drafts','Drafts']].map(([value,label])=><button key={value} className={filter===value?'active':''} onClick={()=>setFilter(value)}>{label}</button>)}</div><div className="tools"><label className="search"><Icon name="search" size={16}/><input aria-label="Search articles" placeholder="Search articles…" value={query} onChange={e=>setQuery(e.target.value)}/></label><select aria-label="Sort articles" value={sort} onChange={e=>setSort(e.target.value)}><option value="updated">Recently updated</option><option value="date">Publication date</option><option value="title">Title A–Z</option></select></div></div><div className="table-wrap"><table><thead><tr><th>ARTICLE</th><th>STATUS</th><th>PUBLISHED ON</th><th>READ TIME</th><th/></tr></thead><tbody>{shown.map(p=><tr key={p.id} onClick={()=>!busy&&open(p.id)}><td><div className="article-cell"><span className="article-symbol"><Icon name="posts" size={21}/></span><div><button disabled={busy} onClick={e=>{e.stopPropagation();void open(p.id);}}>{p.title}</button><p>{p.description||`${p.id}.md`}</p><div className="tag-list">{p.tags.slice(0,3).map((tag,i)=><span key={i}>{tag}</span>)}</div></div></div></td><td><span className={`badge ${p.draft?'draft':'published'}`}><b/>{p.draft?'Draft':'Published'}</span></td><td className="muted">{date(p.pubDate)}</td><td className="muted">{Math.max(1,Math.ceil(p.words/200))} min read</td><td><Icon name="arrow" size={15}/></td></tr>)}</tbody></table></div>{(loading||!shown.length)&&<div className="empty"><span className="empty-icon"><Icon name="posts" size={30}/></span><h3>{loading?'Opening your writing space…':query?'No matching stories':filter==='drafts'?'Room for your next idea.':'Your next chapter starts here.'}</h3><p>{loading?'Reading articles from your repository.':query?'Try a different title, description, or tag.':'Turn a thought into something worth sharing.'}</p>{!loading&&!query&&<button className="secondary" onClick={()=>open()}><Icon name="plus"/> Write an article</button>}</div>}<div className="table-footer"><span>{shown.length} {shown.length===1?'article':'articles'}{query||filter!=='all'?` of ${posts.length}`:''}</span><span><span className="local-dot"/> In sync with your local files <button aria-label="Refresh articles" onClick={()=>refresh().catch(e=>setError(e.message))}>↻</button></span></div></div>{issues.length>0&&<div className="alert"><div><strong>Some files need attention</strong>{issues.map(issue=><p key={issue.id}>{issue.id}.md: {issue.message}</p>)}</div></div>}<div className="ownership"><span>✳</span><div><strong>Good writing needs a little room.</strong><p>Your ideas. Your files. Your pace. Everything you write lives right in your repository.</p></div><span className="ownership-end">Make it yours ↗</span></div><footer className="content-footer"><span>Made for the way you write.</span><span>MARKDOWN AT HEART <span>✧</span> LOCAL BY NATURE</span></footer></section>}
  </div></div>;
}
createRoot(document.getElementById('root')!).render(<App/>);
